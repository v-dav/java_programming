package four;

import javax.swing.*;
import java.awt.*;

class ButtonReset extends JButton {
    public ButtonReset() {
        super("Reset");
        setEnabled(true);
        setName("ButtonReset");
        setBackground(Color.YELLOW);
        setOpaque(true);
    }
}